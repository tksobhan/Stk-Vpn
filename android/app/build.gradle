plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.tksobhan.v2ray_stk"
    compileSdk = 35

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.tksobhan.v2ray_stk"
        minSdk = 23
        targetSdk = 35
        versionCode = 1
        versionName = "1.0.38"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
dependencies {
    implementation 'org.apache.commons:commons-compress:1.26.0'
}
dependencies {
    implementation 'org.apache.commons:commons-compress:1.26.0'
}
dependencies {
    implementation 'org.apache.commons:commons-compress:1.26.0'
}
dependencies {
    implementation 'org.apache.commons:commons-compress:1.26.0'
}
